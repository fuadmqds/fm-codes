#include <bits/stdc++.h>
#define fi first
#define se second
#define Bismillah ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)
using namespace std;

int main() {
    Bismillah;
    
    int n;
    while (cin >> n) {
        if (n == 0) return 0;

        vector<pair<int, int>> v(n);
        for (int i = 0; i < n; i++) {
            int d, m, y, c;
            cin>>d>>m>>y>>c;

            
        }
    }
    return 0;
}
// FM